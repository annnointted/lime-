[server]
host = "0.0.0.0"
port = 3000
workers = 1
max_body_size = 10485760

[database]
host = "localhost"
port = 5432
username = "limeuser"
password = "limepass123"
database = "lime"
max_connections = 10
min_connections = 2
connect_timeout = 10


[auth]
jwt_secret = "my-secret-key-change-in-production"
token_expiry = 3600
refresh_token_expiry = 604800

[redis]
url = "redis://localhost:6379"
max_connections = 5

[app]
name = "lime-service"
environment = "production"
log_level = "info"
